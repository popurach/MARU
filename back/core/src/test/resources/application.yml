spring:
  profiles:
    active: test
    group:
      test:
        - s3-local
        - db-test
        - global-dev
        - security-dev
    include:
      - s3
      - db
      - global
      - security